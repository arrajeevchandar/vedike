import { AdminPage } from "@/components/admin/admin-page";import { archiveCompetitionAction,saveCompetitionAction } from "@/app/admin/actions";import { getDashboardData } from "@/lib/data";
export default async function AdminCompetitionsPage(){const data=await getDashboardData();return <AdminPage><div className="eyebrow">Content management</div><h1 className="page-title" style={{fontSize:48}}>Competitions</h1><details className="glass" open style={{borderRadius:18,padding:22,marginBottom:25}}><summary className="display" style={{fontWeight:700,cursor:"pointer"}}>+ Create Competition</summary><form action={saveCompetitionAction} style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:"0 14px",marginTop:14}}><label className="form-label" style={{gridColumn:"1/-1"}}>Parent event</label><select className="field" name="eventId" required style={{gridColumn:"1/-1"}}>{data.events.filter(e=>!e.isShowcase).map(e=><option value={e.id} key={e.id}>{e.title}</option>)}</select><label className="form-label" style={{gridColumn:"1/-1"}}>Title</label><input className="field" name="title" required style={{gridColumn:"1/-1"}}/><label className="form-label" style={{gridColumn:"1/-1"}}>Description</label><textarea className="field" name="description" rows={3} required style={{gridColumn:"1/-1"}}/><label className="form-label" style={{gridColumn:"1/-1"}}>Rules (one per line)</label><textarea className="field" name="rules" rows={3} style={{gridColumn:"1/-1"}}/><div><label className="form-label">Starts (IST)</label><input className="field" type="datetime-local" name="startsAt" required/></div><div><label className="form-label">Ends (IST)</label><input className="field" type="datetime-local" name="endsAt" required/></div><div><label className="form-label">Entry limit</label><input className="field" type="number" min="1" max="100" name="maxEntries" defaultValue="1"/></div><button className="btn btn-primary" style={{gridColumn:"1/-1",marginTop:18}}>Create Competition</button></form></details><div style={{display:"grid",gap:12}}>{data.competitions.map(c=><div key={c.id} className="glass" style={{padding:18,borderRadius:15,display:"flex",justifyContent:"space-between",gap:14,alignItems:"center",flexWrap:"wrap"}}><div><b className="display">{c.title}</b><div style={{fontSize:12,color:"var(--muted)",marginTop:4}}>{c.isShowcase?"Showcase · payments disabled":`${c.lifecycle} · ${c.maxEntriesPerParticipant??"Unlimited"} entries per participant`}</div></div>{!c.isShowcase&&<form action={archiveCompetitionAction}><input type="hidden" name="id" value={c.id}/><button className="btn btn-secondary" style={{padding:"8px 14px",color:"#ff8a8f"}}>Archive</button></form>}</div>)}</div></AdminPage>}
