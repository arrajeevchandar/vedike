import { AdminPage } from "@/components/admin/admin-page";import { completeCompetitionAction } from "@/app/admin/actions";import { getDashboardData } from "@/lib/data";
export default async function AdminLeaderboardPage(){const {competitions}=await getDashboardData();return <AdminPage><div className="eyebrow">Winner operations</div><h1 className="page-title" style={{fontSize:48}}>Leaderboard</h1><p className="muted">Completion enters a settlement phase, reconciles pending PhonePe orders and then locks the top three.</p><div style={{display:"grid",gap:12,marginTop:25}}>{competitions.map(c=><div className="glass" key={c.id} style={{padding:18,borderRadius:15,display:"flex",justifyContent:"space-between",alignItems:"center",gap:15,flexWrap:"wrap"}}><div><b className="display">{c.title}</b><div style={{fontSize:12,color:"var(--muted)",marginTop:4}}>{c.isShowcase?"Showcase podium":c.lifecycle}</div></div>{!c.isShowcase&&c.lifecycle==="PUBLISHED"&&<form action={completeCompetitionAction}><input type="hidden" name="id" value={c.id}/><button className="btn btn-primary" style={{padding:"9px 16px"}}>Close & Reveal Winners</button></form>}</div>)}</div></AdminPage>}
