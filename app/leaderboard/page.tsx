import { PublicShell } from "@/components/public/public-shell";import { LiveLeaderboard } from "@/components/public/leaderboard";import { getCompetition, getCompetitions } from "@/lib/data";
export default async function LeaderboardPage(){const comps=await getCompetitions();const selected=comps.find(c=>c.submissionCount>0);const detail=selected?await getCompetition(selected.slug):null;return <PublicShell><div className="shell page-pad"><div className="eyebrow kannada">ಮತಪಟ್ಟಿ · Rankings</div><h1 className="page-title">Community <span className="gradient-text">Leaderboard</span></h1><p className="muted">Rankings update with every verified ₹2 vote.</p>{detail?<div style={{marginTop:35}}><h2 className="display">{detail.title}</h2><LiveLeaderboard slug={detail.slug} initial={detail.leaderboard}/></div>:<div className="empty">No competitions with entries yet.</div>}</div></PublicShell>}
