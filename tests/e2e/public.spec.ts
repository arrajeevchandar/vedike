import {test,expect} from "@playwright/test";test("home and public showcase are navigable",async({page})=>{await page.goto("/");await expect(page.getByRole("heading",{name:/Where Culture Meets Community/i})).toBeVisible();await page.goto("/events");await expect(page.getByRole("heading",{name:/Community Events/i})).toBeVisible();await expect(page.getByText("Kannada Sangama 2026")).toBeVisible();await page.goto("/competitions/rangoli-art-challenge");await expect(page.getByRole("heading",{name:"Rangoli Art Challenge"})).toBeVisible();await expect(page.getByText("Showcase only").first()).toBeDisabled()});
