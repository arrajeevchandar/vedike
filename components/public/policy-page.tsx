import { PublicShell } from "./public-shell";
export function PolicyPage({eyebrow,title,children}:{eyebrow:string;title:string;children:React.ReactNode}){return <PublicShell><article className="shell page-pad" style={{maxWidth:850}}><div className="eyebrow">{eyebrow}</div><h1 className="page-title">{title}</h1><div className="glass" style={{padding:"clamp(22px,5vw,48px)",borderRadius:24,lineHeight:1.8,color:"var(--muted)"}}>{children}</div></article></PublicShell>}
export function LegalDetails(){return <p><strong style={{color:"var(--cream)"}}>{process.env.LEGAL_BUSINESS_NAME??"Vedike Community"}</strong><br/>{process.env.LEGAL_ADDRESS||"Business address must be configured before production launch."}<br/>Support: {process.env.SUPPORT_EMAIL??"support@vedike.in"}{process.env.SUPPORT_PHONE?` · ${process.env.SUPPORT_PHONE}`:""}</p>}
