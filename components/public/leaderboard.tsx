"use client";
import { useEffect, useState } from "react";
import type { LeaderboardRow } from "@/lib/types";

export function LiveLeaderboard({slug,initial}:{slug:string;initial:LeaderboardRow[]}){const [rows,setRows]=useState(initial);useEffect(()=>{const id=setInterval(()=>fetch(`/api/competitions/${slug}/leaderboard`).then(r=>r.ok?r.json():null).then(data=>data?.rows&&setRows(data.rows)).catch(()=>{}),5000);return()=>clearInterval(id)},[slug]);return <div style={{display:"grid",gap:10}}>{rows.map(row=><div className="glass" key={row.id} style={{display:"grid",gridTemplateColumns:"44px 1fr auto",alignItems:"center",gap:14,padding:"14px 18px",borderRadius:14}}><b className="display" style={{fontSize:22,color:row.rank===1?"var(--gold)":row.rank===2?"#cfd2dc":row.rank===3?"#cd7f32":"var(--muted)"}}>#{row.rank}</b><div><strong>{row.name}</strong><div style={{height:4,marginTop:8,borderRadius:8,background:"rgba(255,255,255,.08)"}}><span style={{display:"block",height:"100%",width:`${row.percentage}%`,background:"linear-gradient(90deg,var(--red),var(--gold))",borderRadius:8}}/></div></div><b style={{color:"var(--gold)"}}>{row.voteCount.toLocaleString("en-IN")}</b></div>)}</div>}
