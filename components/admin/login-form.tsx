"use client";import {useActionState} from "react";import {loginAction} from "@/app/admin/actions";
export function LoginForm(){const[state,action,pending]=useActionState(loginAction,{});return <form action={action} className="glass" style={{width:"min(410px,100%)",padding:34,borderRadius:25,background:"linear-gradient(165deg,#1c0e12,#100608)"}}><div className="eyebrow">Secure access</div><h1 className="display" style={{fontSize:28,margin:"8px 0 24px"}}>VEDIKE Admin</h1><label className="form-label">Email</label><input className="field" type="email" name="email" defaultValue="admin@vedike.in" required/><label className="form-label">Password</label><input className="field" type="password" name="password" required/><button className="btn btn-primary" style={{width:"100%",marginTop:22}} disabled={pending}>{pending?"Checking…":"Login to Dashboard"}</button>{state.error&&<p role="alert" style={{color:"#ff8a8f",fontSize:13,textAlign:"center"}}>{state.error}</p>}</form>}
